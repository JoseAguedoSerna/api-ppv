version: '3'

services:
  api.laravel:
    build: .
    environment:
      DB_CONNECTION: "mysql"
      DB_HOST: "10.200.4.111"
      DB_PORT: "3306"
      DB_DATABASE: "DocCentral"
      DB_USERNAME: "root"
      DB_PASSWORD: "xQRA0xBC87Gs"
      MAIL_MAILER: "smtp"
      MAIL_HOST: "smtp.gmail.com"
      MAIL_PORT: "465"
      MAIL_USERNAME: "noreply.cecapmex@gmail.com"
      MAIL_PASSWORD: "zjaumwhtrwiewzjq"
      MAIL_ENCRYPTION: "tls"
      MAIL_FROM_ADDRESS: "soportesfytge@gmail.com"
      MAIL_FROM_NAME: "Notificaciones"
    volumes:
      - .:/var/www/html
      - /var/www/html/vendor
      - /var/www/html/public
      - /var/www/html/storage
      - /var/www/html/bootstrap
    ports:
      - 8080:80
    restart: always

